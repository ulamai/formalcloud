GitHub Actions workflows are intentionally disabled.

Active workflow definitions were moved to:

- `.github/workflows-disabled/formal-cloud-gate.yml`
- `.github/workflows-disabled/release-publish.yml`

Move files back into `.github/workflows/` to re-enable them.
